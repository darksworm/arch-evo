# Kiyo powers up in manual exposure with minimum exposure time -> black image
ACTION=="add", SUBSYSTEM=="video4linux", ATTRS{idVendor}=="1532", ATTRS{idProduct}=="0e03", RUN+="/usr/bin/v4l2-ctl -d $devnode --set-ctrl auto_exposure=3"
