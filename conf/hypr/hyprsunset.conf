# No profiles here on purpose: hyprsunset applies profiles unconditionally,
# stomping manual waybar adjustments. The stepped schedule lives in
# sunset-control (daemon mode), which only steps toward redder.
