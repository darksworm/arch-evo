#!/usr/bin/env bash
# Autostart script — kept for reference
# MangoWC handles autostart via exec-once in config.conf
# This file is no longer used directly

echo "NOTE: Autostart is now handled by MangoWC config.conf exec-once directives"
