#!/usr/bin/env bash
# Autostart script — kept for reference
# Hyprland handles autostart via exec-once in hyprland.conf
# This file is no longer used directly

echo "NOTE: Autostart is now handled by Hyprland hyprland.conf exec-once directives"
